#include <iostream>
#include <string>
using namespace std;

int main() {

    int x;
    string s;

    cin >> x;
    cin.get();

    for (int i = 0; i < x; i++) {

        getline(cin, s);
        cout << "Hola, " << s.substr(4, s.length() - 1) << ".\n";

    }

    return 0;
}
