#include <iostream>
using namespace std;

int main() {

	int x, y, res;

	do {

		cin >> x;

		if (x >= 0) {

			cin >> y;

			if (y < 0) break;

			res = (x * 2) + (y * 2);

			cout << res << endl;
		}

	} while (x >= 0 && y >= 0);

	return 0;
}
